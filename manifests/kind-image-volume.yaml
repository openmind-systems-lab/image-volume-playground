kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

name: image-volume-lab

nodes:
- role: control-plane

  kubeadmConfigPatches:
  - |
    kind: KubeletConfiguration
    apiVersion: kubelet.config.k8s.io/v1beta1
    featureGates:
      ImageVolume: true
